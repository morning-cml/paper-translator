# 变更记录（Changelog）

本文件记录每一步优化与各版本之间的区别。遵循[语义化版本](https://semver.org/lang/zh-CN/)。
每条 git 提交是最小记录单元；下方按**里程碑版本**归并，写清"本版相比上一版新增了什么"。

> 说明：项目 2026-07-19 才引入 git，之前有 8 轮迭代（核心引擎）作为「初始提交」
> 一次性入库。故 v0.8 之前的细节见 `程序/docs/交接说明_HANDOFF.md`；
> v0.8 起每一步都有独立 git 提交可追溯。**v1.0.1 是首个打 tag 并公开发布的版本**；
> 更早的里程碑（含 v1.0.0）只在本文档标注、不回溯打 tag——它们的代码早于现在的
> 构建系统，不宜重建，且从未对外分发过。

---

## [1.3.0] — 2026-08-01 · 标题层级修复：识别 → 对齐 → 字号 → 字体

> 版本号 1.2.0 → **1.3.0**：新增输出模式（`--mode updown`）属于功能新增，
> 且随包分发了中文字体（+24 MB），不是修订级改动。

**一句话**：译文里「标题和正文长得一模一样」这件事，根子有四层，逐层挖到底。
起点是用户一句"标题格式没对齐，看着不舒服"，查下去发现根本不是对齐问题。

### 1. 章节标题**根本没被识别**（`pdf_parser.py`）

判粗体只看字体名，正则是 `bold|black|heavy|semibold|-b\b|-bd\b`。而 ACM acmart
模板（CHI / CSCW / UIST 等大量会议在用）的粗体叫 **`LinLibertineTB`**——TeX 系
的 `T`=Text、`B`=Bold 后缀约定，一个都不匹配。后果是**全文零个粗体块**：

- 标题不与正文断开，`2 RELATED WORK 2.1 HCI Research… The research on…` 被当成
  **一个块**送去翻译，输出是一段没有层次的长文；
- 实测输出整页只有 `Song 9pt` 一个字号，标题层级彻底消失。

修法：新增大小写敏感的 `_BOLD_SUFFIX = TBI?$`，另补 `cmbx`（TeX 默认粗体）、
`bx\d`、`[-,_]medi`（TeX 里 Medium 即正文粗体）。

同字号的相邻两级标题另加一条**编号断块**（`_HEADING_NUM`）：acmart 的一级与
二级标题字号**完全相同**（都 10.9pt），只差编号深度，靠字号永远分不开。每级
限 1~2 位数字——不收紧的话 "2020 was a turning point"、"3.14159 is pi" 会被
当成章节号（测试当场抓到）。

### 2. 单行块被系统性缩掉 20%（`layout.py`）——本次最严重的一个

判断「框底之下还放不放得下一行」用的是 `字号 × 1.30`。**但 1.30 是行间距，
单行文本根本不需要它**，于是每个单行块都被凭空多要 30% 的高度：

> 原文 10.9pt 的单行标题，墨迹高 11.1pt。译文只要 21.8pt 宽而框有 100~240pt，
> 宽度绰绰有余，却因为 `10.9 × 1.30 = 14.2 > 11.1` 被判放不下，一路缩到 9.0pt。

**与内容长短无关**，章节标题、图表题注、表格短标签全中招。改为按该行自身的
墨迹高（`_INK_H = 1.12`）判断。大标题从 16.7 回到 17.2，章节标题中位数从
8.91 回到 10.41。

### 3. 对齐（`pdf_parser._mark_alignment` + `layout._flow`）

排版引擎原先只有「从框左边开始排」一种模式，而居中块的框就是它自己的墨迹
范围。中文比英文短约三分之一，于是**每个居中标题都向左偏 (原文宽−译文宽)/2**，
偏移同向且固定。实测大标题偏离栏心 **−30.6pt → ±0.0pt**。

只判居中，不判右对齐：右对齐在论文里极罕见，几何特征却和「首行缩进的两端
对齐段落」几乎一样，误判会把正常段落整段推右。宁可漏，不可错。

压误判花的时间比实现多。三个真实反例（都来自样本论文，不是假想）：

| 版式 | 为什么像居中 | 判别依据 |
|---|---|---|
| acmart `\begin{quote}` 引用块 | 左右各缩进 24pt，中点重合 | 每行**起点相同** |
| 悬挂缩进的编号列表 | 中点差恰好落在容差内 | 只有左边参差 |
| 左对齐的多行标题 | — | 只有右边参差 |

最终判别式是**左右参差量必须相当**，外加 4 行以上一律不判居中（作者块因此也
被排除——它有自己的窄框，按整栏居中会撑破作者网格）。

### 4. 同原始字号 → 同最终字号（`pdf_writer_fitz._heading_start_sizes`）

`layout_block` 是每块**独立**缩字号的，同一级标题会缩成不同大小。改为按原始
字号分组、取组内**众数**统一。⚠️ 取最小值会被一个特别挤的标题拖垮全组：实测
35 个章节标题分布在 `{10.9:6, 10.4:17, 9.9:10, 9.4:1, 8.9:1}`，取最小值会把全部
压到 8.9，与图表题注（8.97）挤成一团，层级反而没了。

**不做 H1/H2/H3 语义推断**——需要一堆启发式且容易错，而"原文一样大的、译文
也一样大"既简单又忠于原排版。

### 5. 真字体取代描边合成（`pdf_writer_fitz.py` + `fonts/`）

内置的 `china-ss` 名不副实：测宽拿到的是 **Droid Sans Fallback**（单字重、无粗体、
非宋体），绘制嵌入的又是 Song CID，两者本就不是同一套字形，且**没有任何粗体
可用**——标题只能描边合成。

改为 `fonts/` 下的思源宋体正文 + 思源黑体 Bold 标题（Noto Serif/Sans SC，
**SIL OFL**，可随 AGPL 分发），由 `fonts/生成字体.py` 从可变字体烘焙。

两个坑：① PyMuPDF 加载可变字体取的是**默认实例**，而 Noto CJK 的默认值在字重轴
最细端（Sans=Thin、Serif=ExtraLight），直接用会让正文变成极细体，比不换更糟；
② `updateFontNames=True` 必须开，否则烘焙出的 Bold 自称 "Thin"，轮廓对、名字错。

⚠️ 实测更正：**描边合成加粗并不"糊"**——标题墨迹占比 6.89% vs 真粗体 6.52%，
肉眼几乎分不出。真正的差别在**正文**：内置字体是黑体，比宋体多 26% 的墨，
整页发闷。换字体的价值在正文可读性，不在标题。

`build.py` 的 `DATA_ITEMS` 原先不含 `fonts` —— 打包版根本不会带字体，一切改进
对下载 exe 的用户等于零。已补。

### 6. 新增「双语·上下对照」输出（`--mode updown`）

左右对照是 2W×H 宽页，在「适合宽度」下只能缩到单页的一半（实测 1.47× vs
2.94×），字小到看不清，而这是它的**物理上限**、调不动。上下对照保持页宽不变，
适合宽度下就是满尺寸。两个回填后端都支持。

（曾想用 PDF 的 `/OpenAction [0 /Fit]` 指定打开即"适合页面"来解决，算完发现
方向反了：适合页面是 1.20×，比适合宽度的 1.47× 还小。方案作废。）

### 测试

新增 `tests/test_heading_detect.py`（38 项）与 `tests/test_alignment.py`（12 项），
都是**纯函数级、不依赖 PDF**——真实论文回归挂在一份不入库的论文上，文件一旦
不在工作区就整批静默跳过，标题识别这种核心不变量必须有 CI 里必然执行的守卫。
另加 `test_updown_makes_tall_pages`。合计 **249 项全通过**。

---

## [1.2.0] — 2026-07-31 · 全面改名 paper-translator + 构建产物「留记录，不留字节」

**一句话**：本地 `release/` 不再无限堆积历史产物——只留每个 profile 的最新一次
构建，更早的自动删除；代价是台账必须先改成**只增不减**，删掉的记录仍在、标
「已清理」，凭来源提交可重建。

> 改名不只是改字符串：程序会**读写以旧名命名的用户数据目录**，故本版含一处
> 真实的运行时行为改动（`_adopt_legacy_dir`，见 §6），配套 4 项新测试。
> 其余为构建/工程侧与文档改动，不碰翻译功能。**198 项测试全通过。**
>
> 版本号从 1.1.0 提到 **1.2.0**：改名波及 exe/目录/产物名并新增数据迁移，
> 不是修订级改动。

### 1. 为什么改

v1.0.1 建的产物保全机制（`_history/` 归档 + 台账入库）动机是"v1.0.0 的 exe 产物
在本机丢失"，方向没错，但策略过死：**任何旧产物都不删**。本机 `release/` 因此攒
到 **1.1 GB**——v1.0.0 的 full 457.8 MB + lite 222.9 MB + `_history/` 428.0 MB。
而这三份全是**从未对外发布过**的 v1.0.0，其中两份 full 还带着"OCR 没打进包"的
缺陷（正是 v1.0.1 修掉的那个）。占着地方，却不会有人用。

### 2. 前提：先让台账扛得住删除

原 `update_index()` 是**照磁盘现存目录重新生成** RELEASES.md 的——产物一删，对应
记录下次构建就跟着消失。而"删了还能重建"恰恰依赖台账记着来源提交，所以直接改成
删，会把"永久留档"这个承诺一起删掉。**这是本次改动的真正难点，不是删文件。**

改为累积式：新增 `release/releases.json` 作数据源（**纳入 git**），`RELEASES.md`
由它渲染。磁盘扫描结果**并入**已有台账而非覆盖，目录没了的记录标「已清理」保留；
以「目录名 + 构建时间」为键，同一版本重建多次也各留一条，不会互相顶掉。台账文件
读不出来时退回磁盘现状重建，不崩。

### 3. 新的保留策略

- `release/` 只保留每个 profile 的最新一次构建，更早的在新构建**自检通过后**删除
  （`--keep-old` 关闭该行为）。
- `--overwrite` 重建同一版本：旧产物先改名暂存到 `.replaced-<名字>-<时间戳>/`，
  等新产物自检通过才真删；构建失败则原样留着可手工还原——不至于人财两空。
- `_history/` 机制取消。

### 4. 本次一并清理

删掉 v1.0.0 的三份本机产物，释放 **1108.8 MB**。三条记录已全部录入新台账并标
「已清理」（含 `_history/` 里那份——它在 `release/` 下没有自己的顶层目录、扫不到，
是手工补录的，否则这条历史会随目录一起消失）。

⚠️ 这三份当初都是**脏工作区**构建，台账里有标记：只能凭提交等价重建、**不保证与
原产物字节一致**。真正对外分发过的 v1.0.1 / v1.1.0 产物在 GitHub Releases 上，
不受影响。

### 5. 全面改名：`pdf-translator` → `paper-translator`

旧名只说得出 PDF，而本工具早已覆盖 Word / PPT / Markdown / TXT / SRT——名字比
真实能力窄，会让人以为其他格式不支持。**仓库名与程序名一并改掉**：

| 项 | 旧 | 新 |
|---|---|---|
| 仓库 | `morning-cml/pdf-translator` | `morning-cml/paper-translator` |
| 程序名（中） | `PDF翻译工具` | `论文翻译工具` |
| 程序名（英） | `PDF Translator` | `Paper Translator` |
| exe / .app | `PDF翻译工具.exe` / `.app` | `论文翻译工具.exe` / `.app` |
| zip 产物 | `pdf-translator-v*.zip` | `paper-translator-v*.zip` |
| 启动脚本 | `启动PDF翻译.bat` / `.command` | `启动论文翻译.bat` / `.command` |
| 窗口标题 / 网页标题 | `PDF 论文翻译` | `论文翻译工具` |
| macOS bundle id | `com.morning-cml.pdf-translator` | `com.morning-cml.paper-translator` |

仓库旧地址 GitHub 自动重定向，已发出的链接不失效。

⚠️ 唯一**故意没跟着改**的是经典界面（`src/gui.py`）标题里的"英译中"——它冻结
于 2026-07-19，早于多语言（B5），**确实只能英译中**。只改了产品名部分，写作
「论文翻译工具 · 经典界面（仅英译中）」，把"这是个受限的备用界面"直接摆到标题上。

### 6. 改名附带的两个坑

**① 用户数据目录不能跟着改名就完事**（`src/paths.py`）

装在 Program Files 或 macOS `.app` 里时，配置和缓存回退到
`%APPDATA%\PDF翻译工具\`（mac 为 `~/Library/Application Support/`）。目录名直接
改掉 = 老用户的 **API Key 要重填、翻译缓存全部作废**——而缓存是真花过钱的，
README 承诺的"同文档重跑几乎零成本"就靠它。

故新增 `_adopt_legacy_dir()`：首次以新名启动时，把旧名目录 `rename` 成新名。
同盘改名是原子操作、不复制字节，几百 MB 缓存也是瞬间完成；**改名失败（无权限 /
被占用 / 跨卷）就继续用旧目录**，宁可名字不一致也不丢数据。

⚠️ `paths.APP_NAME` **故意不从 `version.py` 导入**：产品名可以随便改，硬盘上
已存在的目录名不行。两者绑死的话，下次再改名又会悄悄清空所有人的 Key 和缓存。
以后再动程序名，必须往 `_LEGACY_APP_NAMES` 里追加一条。

**② zip 名必须是 ASCII**：`ZIP_SLUG` 由 `APP_NAME_EN` 派生（`Paper Translator`
→ `paper-translator`），正好与新仓库名一致。若直接用中文名，GitHub Releases 会把
非 ASCII 字符剥光，`论文翻译工具-v1.2.0-full.zip` 到发布页只剩 `-v1.2.0-full.zip`
（v1.0.1 踩过这个坑）。

### 7. 顺手修掉的文档陈述过期

`程序/README.md` 与《使用说明.html》仍写着"把**英文**文档翻译成**中文**"、格式清单
漏了 PowerPoint、测试数还停在 179——都是能力长了、文档没跟上。现已改为
"源语言 11 种（含自动检测）× 目标语言 10 种"、补上 `.pptx`、更正为 198 项。
`程序/docs/项目总览.md` 的"把英文文档译成中文"同此修正。仓库简介同步补上
macOS（此前只写 Windows）。

`src/__init__.py` 里另有一个**和 `version.py` 打架的 `__version__ = "0.1.0"`**
（无人引用、静静过期两年多），改为从 `version.py` 转发，版本号回到单一真源；
其模块清单也早已停留在"只有 DeepSeek + tkinter"的年代，一并更新。

`build.py` 构建成功后的收尾 `print` 用了 `✔`：Windows 控制台默认 GBK 没有该
码位，**构建明明成功却抛 UnicodeEncodeError、以 1 退出**，看着像失败（CI 设了
`PYTHONUTF8=1` 才躲过，本地必踩）。改为 ASCII 标记。

早期记录里的旧名（v0.9「版本管理」条、`未来路线图.md` 第 0 步、
`交接说明_HANDOFF.md` 按日期记录的正文）**一律保持原样**——它们描述的是当时
确实叫那个名字的历史事件，改了反而对不上当时的提交。

---

## [1.1.0] — 2026-07-29 · macOS 支持 + 稳健性修复

**一句话**：新增 **macOS 版**——与 Windows 同源构建、同一次 CI 同步发布，Mac 用户
首次有官方产物可下；并把翻译流程里几处"卡住 / 搞坏 / 白跑"的坑一次补齐——取消能
立刻停、缓存坏了能自愈、翻译前先验通、产物永不半截损坏。稳健性四项均带回归测试
（共 **194 项**全过）。

### 1. macOS 支持（本版主角，也是升次版本号而非修订号的原因）

新增平台属于新功能，故 1.0.1 → **1.1.0**。此前 macOS 只是"能从源码跑"，
**v1.0.1 的发布页上只有 Windows 的 full / lite 两档**——README 虽已写了 macOS
说明，Mac 用户到发布页却下载不到任何东西。本版补上这个缺口。

- **打包**：`build.py` 新增 mac 分支——`.icns` 图标、`--osx-bundle-identifier`、
  `.app` 归置、用 `ditto` 压缩以保留签名、递归自检；全部以 `sys.platform` 门控，
  Windows 路径一行未变。`requirements.txt` 在 mac 上追加
  `pyobjc-framework-WebKit`（pywebview 原生窗口后端）。
  注：PyInstaller 不交叉编译，mac 包只能在 mac 上产出，验证走 CI 的 macOS runner。
- **发布**：CI 构建矩阵从「仅 Windows」扩为 `windows-latest × macos-latest`，
  各出 full / lite，共 4 个产物一次发完。mac 的 zip 名带架构后缀
  （`-mac-arm64`），与 Windows 包不撞名。
- **运行时跨平台**：`webui.py` 的 `os.startfile` 换成跨平台 `_os_open`
  （mac 用 `open`、Linux 用 `xdg-open`）；`paths.py` 在便携目录不可写时的回退
  路径按平台取（mac `~/Library/Application Support`、Linux `~/.local/share`）。
- **源码启动脚本**：新增 `启动PDF翻译.command`（对应 Windows 的 `.bat`）。同时用
  `.gitattributes` 规范行尾——`.bat` 用 CRLF、`.command`/`.sh` 用 LF，后者若带
  CR 在 mac/Linux 下直接无法执行。
- **文档**：新增《程序/docs/Mac使用说明.md》，面向非技术用户讲首次打开
  （Gatekeeper 放行、提示「已损坏」时用 `xattr` 解除、无需 API Key 的离线试用）；
  README 下载区拆成 Windows / macOS 两节，macOS 升为一等公民。
- **Intel Mac 暂不出包**：曾把 `macos-13` 加入矩阵，实测该 runner 排队 18 分钟
  仍未开始、会拖垮整条发布流水线，故撤下；Intel 用户暂从源码运行。产物名已按
  架构区分，日后要恢复只需把 `macos-13` 加回矩阵。

### 2. 取消立即生效（原来"按了没反应"）

`translator.translate_texts` 旧实现把所有批一次性提交线程池，`with` 退出即
`shutdown(wait=True)`——取消要等全部在途 + 排队请求跑完才停（并发 32 时很久）。
改为：队列里尚未启动的批检测到取消即原样返回、不再发请求；收结果循环即时
`break`；executor 改 `shutdown(wait=False, cancel_futures=True)` 不等在途。前端
点取消即时禁用按钮 + "正在取消…"，不再像没反应。实测整篇 1.5s 的活取消后 0.3s 返回。

### 3. 所有产物原子写出 + 精准清理临时残留

PDF / Word / PPT / 文本原来直接写最终路径，中途失败或被取消会在目标处留下
打不开的半截文件（也是"重译同一篇却生成不出来"的元凶之一）。改为先写 `.part`、
成功才 `os.replace` 落位；失败即删、绝不留半截；最终文件被占用（正被 PDF 阅读器
打开）时自动换 `X (1).pdf` 保住成果并回传实际路径。启动时 `sweep_temp`、缓存加载
时删残留 `.tmp`——只删本程序约定命名（`.tmp`/`.part`）的文件，绝不递归/通配。

### 4. 翻译前必做 API 联通性预检

原来只有手动"测试连接"按钮，正式翻译前什么都不查——Key 错 / 连不上会让每一批都
失败、白跑一整篇还产出一份未翻译的文档。现 webui 每个任务、CLI 每次运行翻译前
**强制预检**（离线 mock 跳过），失败点名具体服务（DeepSeek / Kimi / 智谱 / 豆包 /
OpenAI / Ollama 本地）并中止、不产出文件。

### 5. 缓存自愈 + 忽略缓存重译

- **坏缓存自愈**：读缓存时校验公式占位符，命中值若占位符与原文对不上（损坏 /
  旧版本写入 / 磁盘半写）一律作废重译，不再拿坏译文贴出错位公式。
- **忽略缓存重译**：新增 web 高级选项勾选框、CLI `--refresh-cache`——无视旧缓存
  重译、并用新结果覆盖旧的坏缓存（一次性动作，不落盘持久化）。

### 6. 测试隔离（工程健康）

用例原来会读开发机上真实的 `config.json`，本机存过的 `output_mode=sidebyside`
等设置渗进断言，造成"CI 干净全绿、本机却 6 项红"的名实不符。`conftest` 增加
session 级隔离夹具，把 `CONFIG_PATH` 指到空临时目录、清掉 `DEEPSEEK_*` 环境变量，
用例一律从纯默认配置起步。

对应提交：
- **macOS**：`7555233`(跨平台适配) `6df5aae`(使用说明) `658a17d`(.app 打包 + CI 矩阵)
  `c7f67d9`(产物名区分架构) `40dac2c`(README 下载区 + 撤 Intel)
- **稳健性**：`95cbf57`(取消) `8a3023d`(原子写出) `e74c453`(API 预检)
  `9b41a08`(缓存自愈) `93fb0f2`(测试隔离)

---

## [1.0.1] — 2026-07-20 · 首个公开发布版

**一句话**：修掉一个"看不见的"严重缺陷（完整版的 OCR 其实没打进包），
把构建产物管起来（只归档不删除），并**将仓库转为公开**、补上门面 README。

**这是第一个真正对外分发的版本**——v1.0.0 虽已完成全部功能，但从未打 tag、
从未发布，其构建产物还带着下面这个 OCR 缺陷。

### 1. 修复：完整版（full）实际不具备 OCR 能力 ⚠️

打包产物里没有 `rapidocr_onnxruntime` 包，也没有它的三个 `.onnx` 模型。但它
**表现得毫无异常**：exe 正常启动、界面正常、体积还比 lite 大 150MB
（`onnxruntime` 与 `cv2` 是被 `layout_model.py` 的函数内 import 捎带进去的），
所以从构建日志和体积上完全看不出问题。实际后果：full 与 lite 对扫描版 PDF
表现一模一样，那 150MB 白占。

- **根因**：`src/ocr.py` 里 rapidocr 是函数内延迟 import，PyInstaller 静态分析
  扫不到；且其模型与 `config.yaml` 是**包数据**，即便加 `--hidden-import` 也不会
  被带上，必须 `--collect-all`。
- **修复**：full 档增加 `--collect-all rapidocr_onnxruntime`。
- **防复发**：新增 `verify_build()` 打包后自检——full 必须含 rapidocr 包与 ≥2 个
  模型、lite 必须不含，两档都验 exe 与前端资源，不达标直接判构建失败。这类
  "看着对、实则少功能"的缺陷只能靠打包后验产物内容发现。
- 顺带修：缺 OCR 时的提示分场景。打包版用户没有 pip，原来那句"请运行
  `pip install rapidocr-onnxruntime`"对他们是死路，改为提示换用完整版。

### 2. 仓库转公开 + 根目录 README

起因是要把 exe 发给别人：AGPL 要求接收者能拿到对应源码，而包内《首次使用
请看我.txt》指向的地址必须真的打得开，否则分发即不合规。

- 新增**根目录 `README.md`**：此前只有 `程序/README.md`（面向开发者讲架构），
  GitHub 首页是空白的。新 README 面向使用者，含下载入口、full/lite 怎么选、
  "整个文件夹一起用、只拷 exe 跑不起来"的提醒、能力清单与文档地图。
- 转公开前做了安全检查：`config.json`（含真实 Key）全历史从未入库，全历史
  扫描无 `sk-` 形态密钥、无硬编码 api_key/token/password。
- 更新 GitHub 仓库描述（原描述只提"英文文档（PDF/Word）翻译成中文"，已不覆盖
  PPT/Markdown/字幕与多语言方向）并补齐 14 个 topics。

### 3. 构建产物保全

**动机**：v1.0.0 的 exe 产物在本机丢失。查因发现两处：`release/` 被 `.gitignore`
整体排除（git 无法找回），且 `build.py` 每次构建都会 `shutil.rmtree()` 同版本同
profile 的旧目录——**重建一次，上一次的产物就没了**，且无任何提示。

**改动**：
- **构建脚本不再删除任何历史产物**。目标目录已存在时**默认中止**并提示改版本号；
  确要重建同一版本需显式 `--overwrite`，旧产物被**移动**到
  `release/_history/<名字>-<时间戳>/` 留档而非删除。`--clean` 也只动 `build/`。
- **新增版本台账 `release/RELEASES.md`**，由构建脚本自动维护并**纳入 git**
  （`.gitignore` 改 `release/*` 并对其单独取反——用 `release/` 会整体排除目录、
  取反不生效）。二进制仍不入库（走 GitHub Releases），但"发过哪些版本、何时、
  由哪个提交构建、多大"永久可查，本地产物丢了也能从对应提交精确重建。
- `--list` 改为同时列出当前版本与 `_history/` 中被顶替的产物，并显示来源提交。

**产物**：full 314.8 MB（rapidocr 15.5 MB 完整入包，det/rec/cls 三个模型齐全）、
lite 149.6 MB（确认不含 OCR）。两版均实测启动正常并返回 HTTP 200，自检通过。
pytest 179 项全过。

对应提交：`ad9260b`(产物保全) `34edc51`(OCR 修复+自检) `dcd495e`(转公开+README)

---

## [1.0.0] — 2026-07-20 · 正式版

**一句话**：从"英译中 PDF 工具"成长为**多格式、多语言、有测试、可打包、已开源**的
完整文档翻译器。

**相比 v0.9 新增**：
- **多语言方向（B5）**：源 11 种（含自动检测）× 目标 10 种（中/英/日/韩/德/法/西/俄/
  葡/意）。此前只有英→中。含脚本感知断行（欧洲重音不断词、CJK 逐字、韩俄按词）、
  目标语感知的"是否已翻译"判定、按语言对的质量长度带、缓存键含语言对。
- **语言→模型推荐**：界面内联提示，选目标语后建议更合适的服务并可一键切换
  （中英用 DeepSeek、欧洲语言用 OpenAI…）。
- **PowerPoint（.pptx）支持（A3）**：文本框、表格、演讲者备注，样式保真。
- **精简版（lite）构建**：不含 OCR，体积约为完整版一半（149MB vs 298MB）。
- **首次运行引导**：未配置 Key 时给三选一引导页（DeepSeek 申请 / Ollama 免费离线 /
  离线试用），而非等点击后报错。
- **CI（GitHub Actions）**：推送自动跑测试；打 `v*.*.*` tag 自动构建 full+lite 并发布。
- 修复：build.py 的 full/lite 同版本号互相覆盖（改为 `release/v<版本>-<profile>/`）；
  中→英/日→中"原样返回"bug（`_worth_translating` 曾写死"已是中文就跳过"）。

**支持格式**：PDF · Word(.docx) · PowerPoint(.pptx) · Markdown · TXT · SRT
**支持服务**：DeepSeek · Kimi · 智谱GLM · 豆包 · OpenAI · Ollama本地 · 自定义
**测试**：179 项 pytest（CI 干净环境 168 过 + 11 跳过依赖真实论文的用例）

对应提交：`2ece5d7`(lite+引导) `732fb85`(CI) `72ac8f5`(多语言) `a063062`(PPTX)

---

## [0.9] — 2026-07-19 · 加固与扩展（未打 tag）

**相比 v0.8 新增**：把"能用的脚本"变成"可协作、可分发的工程"。

- **自动化测试套件（E1）**：pytest，把此前散落的手工验证沉淀为可重复回归
  （解析结构不变量、缓存、失败降级、占位符保护、跨栏配对、端到端）。
- **表格逐单元格翻译（B1）**：一格=一个翻译单元，译文不越格，表格结构不崩。
- **Word 文档（.docx）支持（A1）**：按 run 就地替换，标题层级/项目符号/表格/
  页眉页脚天然保留。
- **原生应用窗口（C2）**：pywebview，去掉黑窗口与浏览器地址栏，失败自动回退浏览器。
- **译文质量自检与定向重译（B3）**：截断/啰嗦/数字错漏/元话语/重复退化五类检查，
  命中则带着具体问题重译一次。阈值按 116 段真实语料标定，零误报。
- **Markdown / TXT / SRT 支持（A4）**：代码块、链接地址、时间轴等用占位符保护，
  绝不被改动。
- **一键打包与版本管理（C1）**：`build.py` 出 exe，免装 Python；版本唯一真源、
  自动图标、SHA256 清单、多版本按目录归档、代码签名钩子。
- **开源：采纳 AGPL-3.0**（路线 A：开源 + 增值服务）。补 `LICENSE` / `NOTICE.md`。
- **版本管理：git init + 推送私有仓库** `morning-cml/pdf-translator`。

对应提交：`f0b942d`(测试+表格) `7a6b20b`(Word) `96613a4`(应用窗口)
`4e2fcca`(质量自检) `9e24057`(AGPL) `9050af0`(MD/TXT/SRT) `d1b0c80`(打包)
`db90ec2`(整理+项目总览) `b85330d`(版面模型保留决定)

---

## [0.8] — 2026-07-19 · 核心引擎（初始提交 `3b9aeed`，含 8 轮 pre-git 迭代）

首次入库时已具备的能力（此前 8 轮迭代累积，细节见交接说明）：

1. **精确回填内核**：PyMuPDF 精确抹除原文字符（深色背景零露白）+ reportlab 覆盖兜底，
   自动选择与回退。
2. **行内公式保护**：检测 → `⟦Fn⟧` 占位 → 译后校验 → 矢量回贴。
3. **共享重排引擎**：中文断行禁则、逐行避障绕图、向下扩展、自适应缩号、两端对齐。
4. **结构还原**：数据驱动分栏、标题层级（加粗+原色）、参考文献免译保排、跨栏断句缝合。
5. **扫描版 OCR**：RapidOCR 本地离线，无需显卡。
6. **可选版面分析模型**：DocLayout-YOLO（识别表格/图区/独立公式）。
7. **翻译侧**：六家服务预设、术语库、全文语境注入、持久化缓存、失败降级、成本预估、
   试译模式（只翻前 N 页）。
8. **三种输出**：纯中文 / 双语·前后页 / 双语·左右对照宽页。
9. **网页皮肤界面**：分层前端架构，皮肤=一个 CSS 文件（含海绵泡泡、清爽白两套）。
   tkinter 版冻结为备用。

> 这 9 项对应交接说明里的「第一~八轮迭代」，是本项目的技术底座。

---

## 记录约定

- **每步优化**：均有独立 git 提交（`git log` 可查全部 15 条），提交信息写清动机、
  做法、验证结果与踩过的坑。
- **版本区别**：见本文件各版本的"相比上一版新增"。
- **未来计划**：见 `程序/docs/项目总览.md`（现状+遗留+按性价比排序的未来任务）。
- **合规**：依赖许可见 `程序/docs/第三方许可.md`；本项目为 AGPL-3.0。
